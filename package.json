{
  "name": "lhh-fire-watch-binder",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs --check",
    "test:visual:update": "node scripts/build.mjs --update-visuals",
    "preview": "node scripts/preview.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "puppeteer": "24.16.0"
  }
}
